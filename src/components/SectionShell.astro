---
interface Props {
  id: string;
  title: string;
  subtitle: string;
}

const { id, title, subtitle } = Astro.props;
---

<section class="section" id={id}>
  <div class="section-head">
    <h2>{title}</h2>
    <p>{subtitle}</p>
  </div>
  <slot />
</section>
